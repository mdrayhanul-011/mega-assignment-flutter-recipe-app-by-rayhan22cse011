import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary teal/blue-green accent matching the reference cooking app
  static const Color primary = Color(0xFF129575);
  static const Color primaryLight = Color(0xFFE8F5F2);
  static const Color primaryDark = Color(0xFF0E7A60);

  // Secondary accent
  static const Color secondary = Color(0xFFFF9E00);
  static const Color accent = Color(0xFFFF6B6B);

  // Background & Surfaces
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);

  // Borders & Dividers
  static const Color border = Color(0xFFE5E7EB);
  static const Color searchBarBg = Color(0xFFF3F4F6);
}
